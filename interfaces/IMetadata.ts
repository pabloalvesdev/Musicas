interface IMetadata {
  title: string;
  artist: string;
  album: string;
  albumArtist: string;
  genre: string;
  duration: string;
  year: string;
  trackNumber: string;
  discNumber: string;
}

export default IMetadata;
